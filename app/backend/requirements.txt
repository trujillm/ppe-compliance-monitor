flask
flask-cors
pydantic
opencv-python
minio
ovmsclient
torch
# Required by deep-sort-realtime's default MobileNetV2 embedder (used for person
# tracking). The embedder imports torchvision.models.mobilenet_v2 internally;
# deep-sort-realtime does not declare it as a dependency, so we add it explicitly.
torchvision
psycopg2-binary
deep-sort-realtime
langchain
langgraph
langchain-openai
langchain-mcp-adapters
